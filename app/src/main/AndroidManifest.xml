<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sqlite_data">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/notebook"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/notebook"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login_aktivity"></activity>
        <activity android:name=".Splash_Aktivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".update_Aktivity" />
        <activity android:name=".tampil_Aktivity" />
        <activity android:name=".tambah_Aktivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>